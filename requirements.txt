# 3LINES DataHunter — runtime dependencies
#
# Only top-level packages are listed here. Pip resolves transitives.
# Versions use ranges with upper bounds so the image stays buildable over time
# without surprise major-version breaks.

streamlit>=1.36,<2.0
pandas>=2.2,<4.0
openpyxl>=3.1,<4.0
selenium>=4.20,<5.0
psutil>=5.9,<8.0
python-dotenv>=1.0,<2.0
